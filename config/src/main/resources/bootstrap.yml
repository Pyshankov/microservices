spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Pyshankov/spring-cloud-config
          search-paths: microservice-data
  application:
    name: config
server:
  port: 8888
